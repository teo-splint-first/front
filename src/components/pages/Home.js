import React from "react";
import Template from "../common/Template";

const Home = () => {
  return (
    <Template
      children = {
        <p>home</p>
      }
    />
  );
};

export default Home;
