import React from "react";
import Template from "../common/Template";

const MakeTemplate = () => {
  return (
    <Template
      goBackBtn
      children = {
        <p>make</p>
      }
    />
  );
};

export default MakeTemplate;
