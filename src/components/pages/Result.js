import React from "react";
import Template from "../common/Template";

const Result = () => {
  return (
    <Template
      children = {
        <p>result</p>
      }
    />
  );
};

export default Result;
